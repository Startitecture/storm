CREATE TABLE [workflow].[SignatureType] (
    [SignatureTypeId] INT          NOT NULL,
    [Name]            VARCHAR (50) NOT NULL,
    CONSTRAINT [PK_SignatureType] PRIMARY KEY CLUSTERED ([SignatureTypeId] ASC)
);

