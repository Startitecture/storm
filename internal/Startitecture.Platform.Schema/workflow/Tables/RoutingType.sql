CREATE TABLE [workflow].[RoutingType] (
    [RoutingTypeId] INT          NOT NULL,
    [Name]          VARCHAR (50) NOT NULL,
    CONSTRAINT [PK_RoutingType] PRIMARY KEY CLUSTERED ([RoutingTypeId] ASC)
);

